/*
 * Tailwind CSS configuration has been removed.
 * The project now uses pure CSS instead.
 * 
 * To restore Tailwind CSS, uncomment the configuration below and reinstall dependencies:
 * npm install tailwindcss @tailwindcss/postcss
 * 
 * Original config:
 */

export const content = [];
export const theme = {};
export const plugins = [];
